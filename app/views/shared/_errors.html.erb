<% if obj.errors.any? %>
  <div class="alert alert-danger alert-dismissble fade show" role="alert">
    <div id="error_explanation">
      <h4 class="alert-heading"><%= pluralize(obj.errors.count, "error") %> prohibited this <%= obj.class.name.downcase %> from being saved:</h2>

        <ul>
          <% obj.errors.each do |error| %>
          <li><%= error.full_message %></li>
              <% end %>
         </ul>
      </div>
    </div>
  <% end %>